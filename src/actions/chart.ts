import { createAction } from 'redux-actions';

export const setChartConnection = createAction('CHART/SET_DATA');
export const setNewData = createAction('CHART/SET_NEW_DATA');
export const reset = createAction('CHART/RESET');
